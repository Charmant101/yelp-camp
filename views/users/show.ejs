<% include ../partials/header %>
<div class="container">
 <div class="row">
        <div class="col-md-4">
            <h1><%=user.firstName + " " + user.lastName%></h1>
            <div class="thumbnail">
        <img src="<%= user.avatar %>" alt="responsive image">  
        <div class=caption><a href="mail to:<%=user.email%>"><%=user.email%></a></div>
            </div>
            </div>
            </div>   
            <div class="row">
                    <div class="col-md-8">
                    <%=user.info%>       
                     </div>
                     </div>
               <div>
                <a class= "btn btn-large btn-warning" href="/users/<%=user._id%>/edit">Edit</a>
            <form class= "delete-form" action="/users/<%= user._id %>?_method=DELETE" method="POST">
                <button class="btn btn-small btn-danger">Delete</button>
               </div>
            </form>
</div>
    
    


 <% include ../partials/footer %>
